import math

x = 10  # decimal
x = 0b10  # binar

bin(x)  # binar representation of the number
hex(x)

# plenty of the same methods as in JS, but not increment, decrement :(

# in py w do not have const, only var,
# so we use uppercase to indicate that ver in constant
PI = 3.14

round(PI)  # 3
abs(-123)  # 123

# math module for more math functions than builded
# import moved to the top of the file
floor = math.floor(PI)

print(floor)
